# Copy to keystore.properties (gitignored) and fill in the signing details.
# app/shiroikuma.gradle reads these four keys to sign the release build.
# The whole com.termux shared-UID family (this app, termux-api, termux-x11, termux-gui, Emacs)
# must be signed with ONE keystore — see CLAUDE.md → Hard rules.
storeFile=/home/you/.android-keystores/your-key.jks
storePassword=changeme
keyAlias=youralias
keyPassword=changeme
