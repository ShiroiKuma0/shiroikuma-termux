<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly"
    android:sharedUserId="${TERMUX_PACKAGE_NAME}"
    android:sharedUserLabel="@string/shared_user_label">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <permission
        android:name="${TERMUX_PACKAGE_NAME}.permission.RUN_COMMAND"
        android:description="@string/permission_run_command_description"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/permission_run_command_label"
        android:protectionLevel="dangerous" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.DUMP" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- shiroikuma-termux (Phase 4b): the backup service runs as a foreground dataSync service
         (API 34+ needs the typed permission; harmless below), and the two automation callers must
         be visible for our reply broadcasts' setPackage() to resolve on Android 11+ (contract §4). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <queries>
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>

    <application
        android:name=".app.TermuxApplication"
        android:allowBackup="false"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/application_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.TermuxApp.DayNight.DarkActionBar"
        tools:targetApi="m">

        <activity
            android:name=".app.TermuxActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|density|screenLayout|keyboard|keyboardHidden|navigation"
            android:label="@string/application_name"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:theme="@style/Theme.TermuxActivity.DayNight.NoActionBar"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity-alias
            android:name=".HomeActivity"
            android:exported="true"
            android:targetActivity=".app.TermuxActivity">

            <!-- Launch activity automatically on boot on Android Things devices -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.IOT_LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".app.activities.HelpActivity"
            android:exported="false"
            android:label="@string/application_name"
            android:parentActivityName=".app.TermuxActivity"
            android:resizeableActivity="true"
            tools:targetApi="n" />

        <activity
            android:name=".app.activities.SettingsActivity"
            android:exported="true"
            android:label="@string/title_activity_termux_settings"
            android:theme="@style/Theme.TermuxApp.DayNight.NoActionBar" />

        <!-- shiroikuma-termux (Phase 4): the 白い熊 Termux UI page. Translucent theme so the terminal
             underneath stays started and previews every change live. Exported (Phase 4c) for the
             static launcher shortcut in res/xml/shortcuts.xml; it takes no intent data. -->
        <activity
            android:name=".shiroikuma.ui.ShiroikumaUiActivity"
            android:exported="true"
            android:label="@string/shiroikuma_ui_title"
            android:parentActivityName=".app.TermuxActivity"
            android:theme="@style/Theme.ShiroikumaUi" />

        <activity
            android:name=".shared.activities.ReportActivity"
            android:theme="@style/Theme.MarkdownViewActivity.DayNight"
            android:documentLaunchMode="intoExisting" />

        <activity
            android:name=".app.api.file.FileReceiverActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:resizeableActivity="true"
            android:taskAffinity="${TERMUX_PACKAGE_NAME}.filereceiver"
            tools:targetApi="n">
        </activity>

        <activity-alias
            android:name=".app.api.file.FileShareReceiverActivity"
            android:exported="true"
            android:targetActivity=".app.api.file.FileReceiverActivity">

            <!-- Accept multiple file types when sending. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="image/*" />
                <data android:mimeType="message/*" />
                <data android:mimeType="multipart/*" />
                <data android:mimeType="text/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".app.api.file.FileViewReceiverActivity"
            android:exported="true"
            android:targetActivity=".app.api.file.FileReceiverActivity">

            <!-- Accept multiple file types to let Termux be usable as generic file viewer. -->
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="image/*" />
                <data android:mimeType="text/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name=".filepicker.TermuxDocumentsProvider"
            android:authorities="${TERMUX_PACKAGE_NAME}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name=".app.TermuxOpenReceiver$ContentProvider"
            android:authorities="${TERMUX_PACKAGE_NAME}.files"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="${TERMUX_PACKAGE_NAME}.permission.RUN_COMMAND" />


        <receiver
            android:name=".app.TermuxOpenReceiver"
            android:exported="false" />

        <receiver
            android:name=".app.event.SystemEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".shared.activities.ReportActivity$ReportActivityBroadcastReceiver"
            android:exported="false" />


        <service
            android:name=".app.TermuxService"
            android:exported="false" />

        <service
            android:name=".app.RunCommandService"
            android:exported="true"
            android:permission="${TERMUX_PACKAGE_NAME}.permission.RUN_COMMAND">
            <intent-filter>
                <action android:name="${TERMUX_PACKAGE_NAME}.RUN_COMMAND" />
            </intent-filter>
        </service>

        <!-- shiroikuma-termux (Phase 4b): the sister-app backup-automation contract v2.
             §1 the exported broadcast door (no permission by design: it only writes where told),
             §2a the provider data door (caller verified by name, uid and pinned certificate),
             the foreground dataSync service every export/import runs in, and the three integer
             meta-data 応用管理 reads to know this app can be backed up. -->
        <receiver
            android:name=".shiroikuma.automation.StateExportReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="${applicationId}.action.EXPORT_STATE" />
                <action android:name="${applicationId}.action.LIST_CATEGORIES" />
                <action android:name="${applicationId}.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".shiroikuma.automation.AutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <service
            android:name=".shiroikuma.automation.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <meta-data
            android:name="shiroikuma.automation.contract"
            android:value="2" />
        <meta-data
            android:name="shiroikuma.automation.format"
            android:value="1" />
        <meta-data
            android:name="shiroikuma.automation.min_format"
            android:value="1" />

        <!-- shiroikuma-termux (Phase 4c): the absorbed plugins — Termux:Boot (com.termux.boot),
             Termux:Widget (com.termux.widget) and Termux:Float (com.termux.window), one block, names
             written out in full (the plugins' own manifests used ".X" against THEIR package; ours
             is com.termux). No launcher entries: the widget's info page and the float's launcher
             activity are reached from the 白い熊 Termux UI page / the static shortcuts instead.
             RECEIVE_BOOT_COMPLETED and SYSTEM_ALERT_WINDOW are already declared above. -->

        <!-- Termux:Boot — BOOT_COMPLETED → one JobScheduler job per file in ~/.termux/boot,
             each starting TermuxService ACTION_SERVICE_EXECUTE in the background. -->
        <receiver
            android:name="com.termux.boot.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <service
            android:name="com.termux.boot.BootJobService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <!-- Termux:Widget — the list widget, its RemoteViews service, the launcher's CREATE_SHORTCUT
             picker, the (token-guarded) shortcut launcher, the Device Controls provider (API 30+)
             and the refresh-on-boot/update receiver. -->
        <receiver
            android:name="com.termux.widget.TermuxWidgetProvider"
            android:exported="false"
            android:label="@string/title_shortcut_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/termux_appwidget_info" />
        </receiver>

        <receiver
            android:name="com.termux.widget.TermuxWidgetControlExecutorReceiver"
            android:exported="false" />

        <activity
            android:name="com.termux.widget.TermuxCreateShortcutActivity"
            android:exported="true"
            android:label="@string/title_single_shortcut_name"
            android:theme="@style/Theme.BaseActivity.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.termux.widget.TermuxLaunchShortcutActivity"
            android:exported="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:taskAffinity="com.termux.widget.TermuxLaunchShortcutActivity"
            android:theme="@android:style/Theme.NoDisplay" />

        <receiver
            android:name="com.termux.widget.receivers.SystemEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.MY_PACKAGE_UNSUSPENDED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.termux.widget.TermuxWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service
            android:name="com.termux.widget.TermuxWidgetControlsProviderService"
            android:exported="true"
            android:label="@string/title_termux_shortcuts"
            android:permission="android.permission.BIND_CONTROLS"
            tools:targetApi="r">
            <intent-filter>
                <action android:name="android.service.controls.ControlsProviderService" />
            </intent-filter>
        </service>

        <!-- Termux:Float — the invisible activity that starts the floating window (the static
             "Floating terminal" shortcut's target; exported for the launcher, no LAUNCHER category),
             the overlay-permission prompt, and the foreground service that owns the window. -->
        <activity
            android:name="com.termux.window.TermuxFloatActivity"
            android:exported="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:label="@string/float_app_name"
            android:theme="@android:style/Theme.NoDisplay" />

        <activity
            android:name="com.termux.window.TermuxFloatPermissionActivity"
            android:exported="false"
            android:label="@string/float_app_name"
            android:theme="@android:style/Theme.Material" />

        <service
            android:name="com.termux.window.TermuxFloatService"
            android:exported="false" />


        <!-- This (or rather, value 2.1 or higher) is needed to make the Samsung Galaxy S8 mark the
        app with "This app is optimized to run in full screen." -->
        <meta-data
            android:name="android.max_aspect"
            android:value="10.0" />


        <!-- https://developer.samsung.com/samsung-dex/modify-optimizing.html -->

        <!-- Version < 3.0. DeX Mode and Screen Mirroring support -->
        <meta-data
            android:name="com.samsung.android.keepalive.density"
            android:value="true" />

        <!-- Version >= 3.0. DeX Dual Mode support -->
        <meta-data
            android:name="com.samsung.android.multidisplay.keep_process_alive"
            android:value="true" />

        <meta-data
            android:name="com.sec.android.support.multiwindow"
            android:value="true" />

    </application>

</manifest>
